"""Pre-production validation harnesses.

Offline acceptance tests that run PAAIM against real-industry datasets and
measure how well it detects and triggers incidents — precision/recall against
each dataset's own ground-truth labels. Not a product feature; a QA gate before
production.
"""
